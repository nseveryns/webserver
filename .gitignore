# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Any HTML Files put into project folder to test
*.html

# Test folder for website that shouldn't be uploaded
website/

webserver
